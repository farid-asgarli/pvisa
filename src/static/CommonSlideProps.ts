import { SplideProps } from "@splidejs/react-splide";

export const commonSlideProps: SplideProps = {
  options: {
    autoplay: true,
    rewind: true,
    pagination: false,
    speed: 1000,
  },
  hasTrack: false,
};
